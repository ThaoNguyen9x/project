package com.building_mannager_system.mapper.propertiMapper;

import com.building_mannager_system.dto.requestDto.propertyDto.HandoverStatusDto;
import com.building_mannager_system.entity.customer_service.contact_manager.HandoverStatus;
import org.mapstruct.Mapper;
import org.mapstruct.Mapping;

import java.util.List;
@Mapper(componentModel = "spring")
public interface HandoverStatusMapper {



//        // Map entity to DTO
//        // Map DTO to entity
//        @Mapping(target = "office.id", source = "officeId") // Map officeId to Office entity's ID
//        HandoverStatus toEntity(HandoverStatusDto handoverDto);
//
//        // Map entity to DTO
//        @Mapping(target = "officeId", source = "office.id") // Map Office entity's ID to officeId
//        HandoverStatusDto toDto(HandoverStatus handover);
//
//        // Map lists of entities to lists of DTOs
//        List<HandoverStatusDto> toDtoList(List<HandoverStatus> handovers);
//
//        List<HandoverStatus> toEntityList(List<HandoverStatusDto> handoverDtos);
    }


